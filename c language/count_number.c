#include<stdio.h>
int main ()
{
  int a[1],i,n;
  printf("Enter size of the  array :");
  for (i=0;i<1;i++)
  {
      scanf("%d",&a[i]);
  }
 
   if (a[i]%2==1)
   {
       printf("even in this array");
   }
   else
   {
       printf("odd in this array");
   }
    return 0;
}